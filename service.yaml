apiVersion: v1
kind: Service
metadata:
  name: shopping-service
spec:
  type: LoadBalancer
  selector:
    app: shopping
  ports:
  - port: 8080
    targetPort: 5000
    nodePort: 30036
    protocol: TCP

